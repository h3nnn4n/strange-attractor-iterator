#ifndef X_EQUATION
#define X_EQUATION xn = (a * cos((j * M_PI/180.0) + c)) + b * sinh(y * d) / cosh(x * y * a) + a * cos( x * c - d) + b;
#endif

#ifndef Y_EQUATION
#define Y_EQUATION yn = (b * sin((j * M_PI/180.0) + d)) + c * sinh(x * a) / cosh(x * y * b) + b * sin( y * d - a) + c;
#endif

#ifndef W_EQUATION
#define W_EQUATION
#endif

#ifndef Z_EQUATION
#define Z_EQUATION
#endif

