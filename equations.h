#ifndef X_EQUATION
#define X_EQUATION xn = sin(a * y) + c * cos(a * x);
#endif

#ifndef Y_EQUATION
#define Y_EQUATION yn = sin(b * x) + d * cos(b * y);
#endif

#ifndef W_EQUATION
#define W_EQUATION
#endif

#ifndef Z_EQUATION
#define Z_EQUATION
#endif
